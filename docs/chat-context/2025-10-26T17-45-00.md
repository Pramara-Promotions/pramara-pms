# Chat Context Checkpoint
**Timestamp:** 26/10/2025, 5:45 pm  
**Description:** Office laptop sync, dependency fixes, SKUs debug, notifications softened

---

## Actions Today

- Pulled latest from origin/phase2-execution-control (09a7164)
- Installed missing deps:
  - API: `imap`, `mailparser` (Inbound Email service)
  - Web: `socket.io-client` (Notifications)
- Stabilized dev startup:
  - Added `ENABLE_INBOUND_EMAIL=false` in `.env` and gated the service in `api/index.js`
  - Wrapped notifications list route to return empty set (dev) on error instead of 500
- Environment verification:
  - Added `/api/debug/env` dev endpoint to expose safe DB info
  - Added `[sku:list]` log in `/api/project-skus` to print projectId and row count
- SKUs page fixes:
  - Sanitized attribute layout keys (trim/dedupe/skip empty) on load/save
  - Removed React duplicate key warnings by namespacing keys
  - Confirmed data model fields: `poNumber, code, name, color, type, orderQty, attributes`
- Data sanity check:
  - Inserted 1 demo SKU for `projectId=22` (TEST-DEMO) into current Neon DB to confirm persistence
  - Root cause for missing earlier SKUs: entries were created under a different DB before `.env.cloud` copy
- Tooling for multi-device sync:
  - `docs/DEPENDENCY_SYNC.md` (new) to track newly added packages
  - `scripts/pull-and-setup.ps1` (new) one-click pull + dependency check + env copy
- Migration helper:
  - `api/scripts/migrate-skus.js` to copy ProjectSku rows from an old DB into current Neon (SOURCE_DATABASE_URL)

## How to Verify
- Hard refresh SKUs tab
- Check API terminal for `[sku:list] projectId=... -> N rows`
- Open `http://localhost:4000/api/debug/env` to confirm DB host/name

## Notes
- Notifications websocket can show a transient connect-close in dev before auth; list endpoint now fails-soft to avoid UI noise
- Inbound Email stays disabled in dev until configured in SystemSettings or via env

---

## Next
- If you want earlier SKUs back, provide `SOURCE_DATABASE_URL` and run `node api/scripts/migrate-skus.js <projectId>`
- Optionally persist `imageUrl` field in DB if we want images to survive refresh
